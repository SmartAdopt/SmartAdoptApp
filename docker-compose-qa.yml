services:
  nginx:
    image: nginx:1.27-alpine
    container_name: smart-adopt-nginx-qa
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx-qa.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - frontend
      - backend
    networks:
      - smartadopt-qa
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "nginx", "-t"]
      interval: 30s
      timeout: 5s
      retries: 3

  backend:
    image: ${DOCKER_USERNAME}/backend-qa:qa
    container_name: smart-adopt-backend-qa
    # Puerto interno SOLO: el exterior accede a través de nginx
    expose:
      - "9090"
    environment:
      - ENV=qa
      - POSTGRES_HOST=postgres
      - POSTGRES_PORT=${POSTGRES_PORT:-5432}
      - POSTGRES_DB=${POSTGRES_DB}
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - MONGO_URI=mongodb://${MONGO_USER}:${MONGO_PASSWORD}@mongo:27017/${MONGO_DB:-smartadopt_qa}?authSource=admin
      - SECRET_KEY=${SECRET_KEY}
      - ALGORITHM=${ALGORITHM:-HS256}
      - ACCESS_TOKEN_EXPIRE_MINUTES=${ACCESS_TOKEN_EXPIRE_MINUTES:-30}
    depends_on:
      postgres:
        condition: service_healthy
      mongo:
        condition: service_healthy
    networks:
      - smartadopt-qa
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://localhost:9090/health')\" || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 20s

  frontend:
    image: ${DOCKER_USERNAME}/frontend-qa:qa
    container_name: smart-adopt-frontend-qa
    expose:
      - "80"
    networks:
      - smartadopt-qa
    depends_on:
      - backend
    restart: unless-stopped

  postgres:
    image: postgres:16-alpine
    container_name: smart-adopt-postgres-qa
    expose:
      - "5432"
    environment:
      - POSTGRES_DB=${POSTGRES_DB}
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 5
    volumes:
      - postgres_qa_data:/var/lib/postgresql/data
    networks:
      - smartadopt-qa
    restart: unless-stopped
  mongo:
    image: mongo:7
    container_name: smart-adopt-mongo-qa
    expose:
      - "27017"
    environment:
      - MONGO_INITDB_DATABASE=${MONGO_DB:-smartadopt_qa}
      - MONGO_INITDB_ROOT_USERNAME=${MONGO_USER}
      - MONGO_INITDB_ROOT_PASSWORD=${MONGO_PASSWORD}
    volumes:
      - mongo_qa_data:/data/db
    networks:
      - smartadopt-qa
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "mongosh --username ${MONGO_USER} --password ${MONGO_PASSWORD} --authenticationDatabase admin --eval 'db.adminCommand(\"ping\")' --quiet"
        ]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 20s
    restart: unless-stopped
volumes:
  postgres_qa_data:
  mongo_qa_data:
networks:
  smartadopt-qa:
    driver: bridge