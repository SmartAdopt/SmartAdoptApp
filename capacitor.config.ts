import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'net.programacionwebuce.smartadopt',
  appName: 'SmartAdopt',
  webDir: 'frontend/dist'
};

export default config;
