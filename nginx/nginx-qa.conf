limit_req_zone $binary_remote_addr zone=api_qa:10m rate=20r/s;

upstream backend_qa {
    server backend:9090;
    keepalive 16;
}

upstream frontend_qa {
    server frontend:80;
    keepalive 8;
}

server {
    listen 80;
    server_name smartadoptqa.programacionwebuce.net;

    server_tokens off;

    location /api/ {
        limit_req zone=api_qa burst=30 nodelay;

        proxy_pass         http://backend_qa/;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto https;
        proxy_set_header   Connection        "";

        proxy_connect_timeout 10s;
        proxy_read_timeout    60s;
        proxy_send_timeout    60s;
    }

    location /health {
        proxy_pass         http://backend_qa;
        proxy_http_version 1.1;
        proxy_set_header   Connection "";
        access_log off;
    }

    location /socket.io/ {
        proxy_pass         http://backend_qa/socket.io/;
        proxy_http_version 1.1;

        # Headers requeridos para el handshake WebSocket
        proxy_set_header   Upgrade           $http_upgrade;
        proxy_set_header   Connection        "upgrade";

        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto https;

        # Timeouts extendidos para conexiones WebSocket persistentes
        proxy_read_timeout    3600s;
        proxy_send_timeout    3600s;
        proxy_connect_timeout 10s;
    }

    location / {
        proxy_pass         http://frontend_qa;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   Connection        "";
        proxy_cache_valid 200 1m;
    }

    error_page 500 502 503 504 /50x.html;
    location = /50x.html {
        root /usr/share/nginx/html;
    }
}
